# Database Reset

I've created a script to clear all users from the database as requested. 
Please run the following in your terminal:

```bash
cd backend
source venv/bin/activate
python reset_db.py
```
